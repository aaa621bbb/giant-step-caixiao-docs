# 巨人踩城 v2.0 — 重做方案（给实现者）

> 目标仓库：`aaa621bbb/giant-step-caixiao`（女巨人踩小人，Android WebView + babylon-mmd 网页前端）
> 基线提交：`1f88e9b 踩小人v1.2`（2026-08-10）
> 若你手上的版本比这个更新，本方案依然成立——文中的**架构、数值、验收标准**按新版套用即可。
> 本文档只定义"要什么、为什么、验收线"，不规定具体写法；实现细节由你决定。

---

## 0. 一句话结论

现有版本不是"少个功能"，而是**玩法底层没成型**：尺度系统缺失、相机是错的、输入没接通、城市是占位几何。
**建议重做游戏模块**（城市 / 相机 / 玩家 / 巨人 / UI 五块），保留现有的 MMD 模型加载与 VMD 播放链路不动。

**修好"能不能玩"之后，还要解决"值不值得玩"**：旧版只有一个"被追着踩、活 60 秒"的循环，
内容太薄。因此本方案分两条线并行推进——**§3~§12 修底层**（能不能玩），
**§7.5 补内容**（值不值得玩），内容按 P0/P1/P2 分层，不要一次全铺开。

---

## 1. 现状诊断：9 条反馈 → 根因 → 对策

| # | 你反馈的现象 | 已定位的根因（代码级） | 对策（本方案章节） |
|---|---|---|---|
| 1 | **是竖屏的** | Activity 未锁方向；HUD/CSS 按竖屏 9:16 设计 | §2 锁定横屏 + 横屏 HUD 重排 |
| 2 | **城市建模特别粗糙，只是极简纯色几何体** | 只有 `CreateGround` 一块地 + 少量无材质 primitive | §4 城市美术技术规范（模块化建筑生成器） |
| 3 | **导入不了 PMX 模型** | 模型必须走 `mmdRuntime.createMmdModel()` 才是 MMD 模型；且贴图必须与 PMX 同目录；导入失败时无错误回显 | §8 模型导入管线 + 校验 + 报错 |
| 4 | **UI 按钮太小** | 按钮按桌面鼠标尺寸做，未按移动端触摸目标规范 | §9 HUD 规范（≥48dp，横屏布局） |
| 5 | **进游戏后什么都看不见，视角穿模到几何体** | **玩家眼高写死 0.05（5cm）**，出生点在原点（=地面/建筑所在处），相机必然埋进几何体；无相机碰撞 | §3 尺度系统 + §5 相机碰撞 |
| 6 | **移动不了** | `gdWalkSpeed` 默认 0.10，在缩小世界里近乎静止；输入与渲染循环的接线不稳定 | §6 玩家控制器（速度按尺度推导） |
| 7 | **要第一人称/第三人称能切换** | 只有一条 UniversalCamera 第一人称，无第三人称、无切换 | §5 相机系统（三档 + 切换） |
| 8 | **镜头灵敏度要可调** | 有 `gdSens` 但无独立俯仰灵敏度、无反转、无平滑、无 FOV/机位距离调节 | §5.4 相机设置面板 |
| 9 | **游戏完全不成型，根本玩不了，要重做** | 玩法没有状态机边界、没有生成点校验、没有性能预算、没有可验证的 HUD | §10 架构重构 / §11 性能 / §12 分阶段验收 |
| 10 | **建模要很精致** | 当前无材质、无阴影、无街具、无绿化、无远景 | §4 全节 + §4.9 "精致"的客观验收线 |

**优先级**：§3 尺度 → §5 相机 → §6 输入 → §4 城市 → §9 UI → §7 巨人 AI。
其中 §3/§5/§6 是**"能不能玩"**的门槛，必须先修完并单独验收，否则城市做得再精致也没人看得见。

---

## 2. 重做总原则

1. **横屏优先（Landscape 锁定）**。这是手机游戏品类，且巨人的"压迫感"需要横向视野。
   - Android 侧：Activity 锁 `sensorLandscape`（允许左右横握）。
   - Web 侧：CSS 与 HUD 全部按 `width > height` 布局重排，不用 `100vh` 竖屏假设。
   - 处理刘海/挖孔安全区：用 `env(safe-area-inset-*)`，HUD 元素避开。
2. **五块模块独立、可单独开关、互不残留**：`CityKit` / `PlayerController` / `CameraRig` / `GiantAI` / `HUD`。
   进入/退出游戏模式时逐块 `init()` / `dispose()`，退出后回到原 MMD 播放界面必须**零残留**。
3. **所有可调数值集中到一个 `CONFIG` 对象**（含尺度、速度、相机、AI 参数），改手感只改一处。
4. **不做无贴图纯色**。任何可见物体至少要有：基础色 + 粗糙度 + 一张程序化贴图（细节/凹陷/窗格）。
5. **性能是硬约束**（目标机型红米 K90 / 骁龙 8 Elite）：见 §11 预算表，超预算就砍数量不砍质量。

---

## 3. 尺度系统（**先定这个，否则其余全是错的**）

### 3.1 三个层级
| 层级 | 含义 | 建议值 |
|---|---|---|
| **世界单位** | 全部代码统一用米（m） | 1 unit = 1 m |
| **巨人** | 加载进来的 PMX 按原生尺度，不缩放 | PMX 正常身高 ≈ 15~20 m（视觉设定） |
| **玩家小人** | 明确设定身高 | **1.6 m**（现实人体） |
| **比例差** | 巨人 / 玩家 | **≈ 10~12 : 1** |

> 实现建议：保持 PMX 不动（因为它的物理裙摆、IK、VMD 都是原生尺度，一缩放全崩），
> **把玩家、城市、街具按真实米制尺寸放在巨人脚下**。巨人自然就是巨人。
> （这与"把小人缩小"是同一件事的两种表述，但**必须以"玩家 1.6m"为基准写代码**，比"乘以 0.1"更好推理、更好调。）

### 3.2 硬性要求
- **禁止任何 magic number 直接写死相机眼高/速度**。必须从 `CONFIG.player.height` 推导：
  - 第一人称眼高 = `height * 0.92`（≈1.47 m）
  - 碰撞胶囊半径 = `height * 0.25`，高 = `height`
  - 行走速度 = `height * 2.2` m/s（≈3.5 m/s），奔跑 = ×1.8
- 出生点必须**校验**：先在候选点做一次向下射线 + 周围球形检测，确认不在任何几何体内部、脚下有地面、头顶净空 ≥ 2×身高；不满足就换点重试（最多 N 次），最后兜底到"城市中央广场预设安全点"。
- 相机 near clip 建议 `0.05~0.1`（避免与头发/衣服穿插），far 按城市可视距离（建议 500~800 m）+ 远景层。

### 3.3 验收
- 进入游戏第一帧必须能看见城市与地面（不是纯色/黑屏）。
- 玩家站在街上，头顶到巨人脚踝的视觉比例约 1:10。
- 出生点连续重开 20 次，无一次卡在几何体里。

---

## 4. 城市美术技术规范（"精致"的落地标准）

> 硬要求：**不引入任何外部模型/贴图文件**（保持包体可控、无版权风险）。
> 全部用 babylon 的 `MeshBuilder` + **程序化生成的贴图**（离屏 canvas 画窗格/砖缝/标线，`DynamicTexture`）。
> 精致 ≠ 面数多，而是**层次、重复中的变化、材质、光影**。

### 4.1 建筑生成器（核心）
不要"随机一个盒子"。做一个**模块化建筑生成器**，一栋楼 = 基座 + 标准层 + 顶部，各自独立随机：

- **体量**：宽 8~20 m、进深 8~20 m、高 12~80 m；支持**退台**（上层内缩，形成阶梯轮廓）与**双体量拼合**（让轮廓不是单一立方体）。
- **立面**：程序化窗格贴图——按层高与开间画出窗洞，**随机点亮 15~30% 的窗**（夜景关键）；加窗台线、腰线、竖向分隔条。
- **底座（1~2 层）**：商铺/大堂，独立材质（玻璃门 + 暖色内透光 + 招牌色块 + 遮阳棚），与上部立面明确区分。
- **顶部**：女儿墙（加高 0.5~1 m 的一圈）+ 随机屋顶道具：水箱、空调外机、通风管、天线、电梯机房。
- **变化来源**（避免"一眼 AI 复制粘贴"）：色调从**分区色板**里取（商业区偏冷灰蓝 / 住宅区偏暖米 / 老城区偏黄褐）、窗格密度、退台层数、屋顶道具组合。
- **数量与布局**：主街两侧各 6~10 栋，后排再 1~2 圈做层次；**沿街立面必须贴合街道边界**（不能歪进马路），用栅格抢占的方式布局，保证没有楼压到路面/人行道。
- **远景层**：在 300~500 m 外放一圈**低模剪影楼群**（无窗、单色、受雾影响）撑天际线，近处细节 + 远处剪影 = 城市感。

### 4.2 路面 / 人行道
- **车道**：沥青深灰 + 程序化贴图（车道虚线/实线、磨损、油渍、补丁），主干道 2~4 车道。
- **人行道**：必须**抬高 0.15 m**（路缘石），用不同的铺装格子贴图（砖缝）。这一条"高差"是廉价但极其有效的"城市感"来源。
- **路口**：十字/丁字路口 + **斑马线** + 停止线 + 转向箭头，路面贴花。
- **附属**：井盖、雨水篦子、路缘石坡道（无障碍口）。
- **地面纹理不能靠一块纯色平面**——至少 3 种材质分区（车道 / 人行道 / 广场砖）。

### 4.3 街道设施（决定"精致度"的关键，别省）
沿街**按固定间距自动布置**，每类都要有细节而不是一个色块：
- **路灯**：灯杆（锥台）+ 弯臂 + 灯头；夜间**灯头发光 + 地面投射一小片光斑**（用发光材质 + 贴花，不必真光源）。
- **行道树**：树干（细锥台 + 树皮色）+ 树冠（2~3 层不规则低模簇，或者**十字交叉的带 alpha 的叶片面片**，后者更省面更自然）；树下加树池。
- **信号灯 / 指示牌 / 路牌**：立柱 + 牌面（程序化画指针/文字块）。
- **街道家具**：长椅、垃圾桶、消防栓、公交站牌/雨棚、花坛、护栏（沿路缘成段）。
- **电线杆 + 电线**：老城区段落加，电线用 `LinesMesh` 拉出下垂弧线——成本极低但观感提升明显。
- **广告牌 / 灯箱**：贴一面程序化图形（自发光），夜景最出效果。

### 4.4 绿化
- 行道树（见上）；街角/广场加**灌木丛**（低模簇）与**草地/花坛**（不同色的贴地材质）。
- 可选：飘落的树叶粒子（极少量）增加生气。

### 4.5 天空 / 远景 / 雾
- **天空**：渐变天空（顶部深蓝 → 地平线暖橙/淡青），按时间档切换；或天空盒。
- **时间档**：白天 / 黄昏 / 夜晚三档（夜景是巨人题材最出气氛的，**建议默认黄昏或夜晚**）。
- **雾**：必须开（`FogMode.EXP2`，密度让 300 m 外明显衰减），既出纵深又遮远景接缝。
- **太阳/月亮**：一个可选的天空光斑 + 地面长投影。

### 4.6 光照与阴影
- **主光**：平行光做太阳，**必须开阴影**（`ShadowGenerator` + 级联/较大 shadow map）。
  巨人投在街道和楼上的**巨大阴影**是"巨人感"的核心来源之一，优先级极高。
- **环境光**：半球光（天空色/地面反射色）避免暗部纯黑。
- **夜景补光**：路灯/招牌的点光或自发光 + 一层暖色调色。
- **接触阴影**：玩家角色脚下加一个软圆贴花，避免"悬浮感"。

### 4.7 材质（不许纯色）
每个材质至少：`albedo`（贴图或分块色）、`roughness`（沥青粗糙、玻璃光滑）、`metallic`（金属街具）。
贴图用 `DynamicTexture` 在加载时程序化画出来（窗格、砖缝、沥青、标线），**一次生成、全场景复用**，不要每个物体各画一张。

### 4.8 实例化与性能
- 重复物体（窗、楼、树、路灯、护栏段）**一律用 thin instances / instances**，不要一个个 `new Mesh`。
- 远景楼群可用合并网格（`mergeMeshes`）。
- 贴图统一分辨率 256~512，开 mipmap。

### 4.9 "精致"的客观验收线（必须逐条打勾）
1. 站在街上看四周：能看到**至少 6 栋造型各异的楼**（不是同一盒子复制）。
2. 楼有**可辨认的窗格**，且**部分窗户亮着**。
3. 人行道与车道**有高差**，路面有**车道线/斑马线**。
4. 街上有**路灯、树、至少两种街道家具**。
5. 有**雾**，远处有**天际线剪影**，天空不是纯色。
6. **巨人的影子投在街上**，玩家角色脚下有接触阴影。
7. 夜晚档能看出"这是晚上"（窗光 + 路灯发光 + 整体色调）。
8. 截图不看 HUD，别人能认出"这是一条城市街道"，而不是"一堆盒子"。

---

## 5. 相机系统（修"看不见/穿模" + 加切换）

### 5.1 三种机位（一个 `CameraRig` 统一管理）
| 模式 | 说明 |
|---|---|
| **FPS 第一人称** | 眼高 = `height*0.92`，抬头能看到巨人全貌（这才是这玩法最爽的视角）。 |
| **TPS 第三人称** | 弹簧臂（spring arm）跟在角色后上方，距离可调 2~8 m，肩后偏移；有角色模型时能看到自己的小人。 |
| **观察/自由** | 环绕轨道，用来欣赏巨人全身（复用现有 ArcRotate 逻辑）。 |

- 切换：HUD 上一个**大按钮**（图标 ⌖ / 👁）循环切换；也支持快捷键。
- 进入游戏默认 **FPS**（沉浸感最强），一键可切 TPS。

### 5.2 相机碰撞（**直接解决"穿模"**）
- TPS：从角色头部向相机目标位置做**射线检测**，命中几何体就把相机拉到命中点之前（留 0.2 m 余量）。
- FPS：`near clip` 调小（0.05~0.1）+ 角色自身网格不参与渲染（或设 `renderingGroupId` 单独渲染），避免看到自己内壁。
- 任何情况下相机**不得低于地面**：加一条"地面高度下限"钳制。

### 5.3 眩晕与手感
- 俯仰限位（约 -85°~+85°），不要翻转。
- 可选**镜头阻尼/平滑**（默认开一点，跟手又不生硬）。
- 移动时轻微 FOV 变化（可选，1~2 度）增加速度感。

### 5.4 相机设置面板（修"灵敏度要可调"）
一个设置抽屉，全部**大滑杆 + 实时生效 + 本地持久化**：
- 水平灵敏度（0.2x ~ 5.0x）
- 垂直灵敏度（**独立**，0.2x ~ 5.0x）
- 反转 Y 轴（开关）
- 视角阻尼 / 平滑（0 ~ 1）
- 视野 FOV（60° ~ 100°）
- TPS 相机距离（2 ~ 8 m）、肩部偏移
- 手柄/触摸摇杆灵敏度（与移动摇杆分开）

---

## 6. 玩家控制器与输入（修"移动不了"）

### 6.1 必须排查的接线问题（按此顺序）
1. **输入有没有真的到引擎**：在 HUD 上放一个**调试读数**（速度、坐标、状态、相机模式），先眼见为实。
2. **渲染循环/observer 是否在跑**（进游戏后 `onBeforeRenderObservable` 是否挂上、`runRenderLoop` 是否唯一）。
3. **速度是否为 0 或过小**：按 §3.2 从身高推导，禁止沿用 0.10 这种近似零的值。
4. **摇杆区域是否被"转视角区"整块盖住**（触屏事件被上层元素吃掉）——这是这类实现最常见的"移动不了"原因。
   → 触控区必须**显式划分且不重叠**：左手摇杆区（屏幕左下 ~35% 宽）、右手视角区（其余），中间留出按钮区。
5. **移动方向基准**：按相机朝向计算（`getDirection`），且只取水平分量（别让角色往天上走）。

### 6.2 移动手感
- 加速/减速插值（不要瞬间启停）。
- 行走/奔跑两档（奔跑消耗或冷却可选）。
- 可选**冲刺/翻滚**（躲避踩踏的核心动作），有短无敌帧。
- 移动时角色朝向平滑转向（TPS 下），FPS 下隐藏自身或只用影子。

---

## 7. 巨人系统（追踩要自然）

- **状态机**：`巡游 → 发现 → 追捕 → 抬脚蓄力 → 踩下 → 收脚 → 巡游`，用**时间驱动**（不用帧数），状态间加过渡时长。
- **追捕**：朝玩家水平移动 + 平滑转向；**头部/视线下压看向脚下的小人**（极出效果）。
- **步态**：优先用现成行走/踩踏 VMD；没有就用程序骨控（大腿/膝/踝交替），关键——
  **步频必须匹配平移速度**，否则脚底打滑，一眼假。
- **踩踏判定**：脚掌挂球体碰撞（随足首骨世界位更新）；判定条件 = **脚掌下落中（Y 递减）且玩家在脚掌水平圆内**。命中后给 1.5~2 s 免伤，避免连踩。
- **演出**：落地扬尘（简单粒子）+ 屏幕震动 + 玩家被踩扁（无敌时改为被震开+踉跄）。
- **无敌模式**：一个显眼开关，开启时命中只演出不判负（现有逻辑保留，确认 UI 可达）。
- **尺度**：巨人移动速度按"跨一步的距离 / 时间"推导，与步态同步；不要用与玩家同量级的速度。

---

## 7.5 玩法内容与模式（**本次重点扩充**）

> 前一版只有一个"被追着踩、活 60 秒"的循环，内容太薄，玩两分钟就腻。
> 这一章给出**内容清单**。注意它不是"一次全做"的要求——按 §7.5.9 的
> **P0 / P1 / P2 分层**推进：先把 P0 做扎实，再逐层加料。
> **判断标准：一个没看过说明的人上手，能自己发现"原来还能这样玩"。**

### 7.5.1 游戏模式（主菜单可切换，至少做 3 种）

| 模式 | 玩法 | 优先级 |
|---|---|---|
| **① 生存逃脱**（默认/主模式） | 在城市里躲巨人追踩，存活计时；有连击/擦身奖励；有无敌开关 | **P0** |
| **② 自由沙盒**（无死亡） | 不判负、巨人不会真的踩死你（=无敌强制开）。用来**逛城市、看巨人、试操控**，是新手引导的天然载体 | **P0** |
| **③ 巨人模式（反转）** | **玩家操控巨人**在街上走，去踩满街的 NPC 小人（踩扁成贴地纸片，带计数/连击）。踩到车会压扁、踩到路灯会踩断 | **P1** |
| **④ 挑战关卡** | 多个短关卡，目标递进：比如"30 秒内从 A 点穿到 B 点"、"躲过 5 次踩踏不受伤"、"在被发现前潜入地铁口" | **P1** |
| **⑤ 拍照 / 摆姿** | 冻结 AI，自由机位 + 让巨人摆姿势，与城市同框出片（与模块 B 打通，见 §7.5.7） | **P2** |

> ① 与 ② 必须做，因为 ② 是唯一能让用户"看清楚这个游戏到底有什么"的模式——
> 前面"什么都看不见、玩不了"的教训告诉我们：**先让人能看见、能走动，才谈得上好玩。**

### 7.5.2 城市内容扩展（从"一条街"到"一座城"）

**分区（P0 做 2 个，P1 做齐）：**
- **商业街**：高层写字楼 + 玻璃幕墙 + 霓虹招牌 + 底层商铺（卷帘门/雨棚/招牌/橱窗）
- **住宅区**：低矮公寓楼 + **阳台/晾衣杆/空调外机/防盗窗**（这三样是"有生活感"的关键）
- **公园**：草地 + 树 + 长椅 + 喷泉 + 儿童设施（滑梯/沙坑/秋千）
- **工地**：塔吊 + 围挡 + 钢筋堆 + 脚手架 + 水泥袋（**可攀爬点**，见 §7.5.3）
- **水岸**：河/运河 + 桥 + 护栏 + 泊船（做一个，立刻拔高城市层次）

**地标（P1，1~2 个足够）**：电视塔 / 钟楼 / 大型商场 / 体育馆。
作用 = 远景锚点，让玩家有方向感，也方便截图出片。

**街道家具清单（P0 至少 8 种，P1 补全）**：
路灯、红绿灯、斑马线、车道线、人行道护栏、公交站牌、**报刊亭**、**电话亭**、
**自动售货机**、长椅、垃圾桶、消防栓、井盖、交通锥、路障、广告灯箱、
霓虹招牌、遮阳棚、楼梯、无障碍坡道。
> 这些不是装饰，是**玩法资源**：垃圾桶可藏身、售货机可推倒挡路、
> 消防栓被踩会喷水柱、交通锥可以被踢飞。

**车辆（P1）**：路边停车（可被踩扁成"纸箱"、可被踢飞）+ 少量行驶车辆。
**车辆被踩扁的瞬间是本玩法最好的"尺度感"演示**——比任何 UI 数字都直观。

**动态元素（P1/P2）**：NPC 行人（见到巨人会尖叫逃散）、飞鸟群（被惊起飞走）、
气球、飘动的旗与横幅、随风滚的纸片与落叶。
> 静态城市是"模型"，有动态才叫"活的城"。

**细节层（P2）**：地面水洼（简化反射）、污渍、裂缝、墙面涂鸦、褪色海报。

**垂直内容（P1）**：地下通道入口 / 地铁站口 / **可达楼顶**。
有了垂直空间，"躲"才有策略（往楼上跑、钻地下）。

### 7.5.3 玩家能力与道具（让"跑"变成"玩"）

**基础动作（P0）**：走 / 跑 / **闪避翻滚**（短距离冲刺 + 短暂无敌帧，是躲踩的核心手感）。
**进阶（P1）**：跳、蹲下（缩小被踩判定）、被震到踉跄（巨人靠近时的被动演出）。

**道具（P1，城市里散落并定时刷新）**：
| 道具 | 效果 |
|---|---|
| 能量饮料 | 短时间加速 |
| 护盾 | 抵挡一次踩踏 |
| 烟雾弹 | 巨人短暂失去目标 |
| **钩爪** | 勾住楼顶/围挡，快速上高处躲避 |
| 诱饵 | 投掷出去引开巨人注意力 |

**藏身机制（P1，潜行玩法的核心）**：
钻进垃圾桶 / 躲到车底 / 进入地下通道 / 跑进楼内 →
巨人**失去目标并进入"搜索"状态**（左右张望、低头找、踩开垃圾桶）。
> 这一条是整个玩法从"体力活"变成"斗智"的关键，强烈建议做。
> 配套：巨人要有**视线锥 + 听觉**（跑步声会引来注意），潜行才有意义。

### 7.5.4 巨人行为扩展（让"追踩"不单调）

- **性格系统（P1）**：不同 PMX 巨人配不同人格，直接影响参数与动作选择——
  **暴躁型**（追得凶、踩得频繁）/ **好奇型**（会蹲下来盯着你看、伸手指戳你）/
  **玩闹型**（踢来踢去、把你当玩具）/ **温柔型**（动作慢、多是恐吓而非真踩）。
  > 用户会换不同 PMX 模型（昔涟/流萤…），性格系统正好让"换模型"产生真实的玩法差异，
  > 而不是同一个游戏换张皮。
- **动作库（P1）**：踩 / 碾（脚掌前后滚）/ 踢 / 抓（捏起来看）/ 蹲下伸手指戳 / 吹气。
- **场景互动（P1/P2）**：踩扁车辆、踩断路灯、踩塌楼顶、踩进水洼溅水、
  踩到消防栓喷水柱、惊起鸟群。**巨人与场景的互动量，直接等于"巨人感"的厚度。**
- **搜索状态（P1）**：失去目标后不立刻重置，而是"原地张望 → 走向最后目击点 → 翻查藏身处"。

### 7.5.5 反馈、成长与收集（给玩家"想再来一局"的理由）

- **计分（P0）**：存活时间 + **擦身而过奖励**（巨人脚掌距你 < 1 m 时给分并弹出提示）
  + 连击。结算页显示历史最佳。
- **局内播报（P1）**：像体育解说一样弹字——"擦身而过！""巨人暴怒了！""完美闪避 ×3"。
  成本极低、爽感极高，性价比第一。
- **成就 / 里程碑（P1）**：首次贴脸躲过、无伤活满 60 秒、靠藏身处脱险 3 次、
  亲眼看着巨人踩扁 5 辆车…（给玩家"有东西可追"）。
- **收集要素（P2）**：城市内隐藏 N 个彩蛋/明信片，集齐解锁新巨人外观或新城市皮肤。
- **慢动作特写（P2，强烈推荐）**：踩中或被擦身的瞬间，切一个**运镜特写**
  （从街角仰拍巨人压下来的脚），放慢 0.3 倍速。
  > 用户做这个游戏的原始动机就是"看巨人踩"——**特写镜头才是核心爽点**，别只做成一个躲闪小游戏。

### 7.5.6 氛围与视听（"精致"的另一半）

- **时间档（P0 已有 → P1 做全）**：清晨 / 正午 / 黄昏 / **夜晚（霓虹 + 窗户点亮）**。
  夜间的城市观感提升是量级的，性价比很高。
- **天气（P2）**：晴 / 雨（地面湿反光 + 雨丝）/ 雾。
- **音效（P1）**：
  - 巨人脚步——**要有方位感与由远及近的层次**（这是压迫感的一半）
  - 落地重低音、地鸣、玻璃碎裂、警报、NPC 尖叫
  - 危险时的心跳声 / BGM 动态切换（安全→紧张→被追）
- **震动反馈（P1）**：`navigator.vibrate` 或 Android 桥——踩下 / 巨人靠近 / 命中。

### 7.5.7 摆姿模块 ↔ 游戏 的联动（P2，但很划得来）

- 在摆姿模式摆好的姿势，可以**直接投放进城市当巨人的一个动作**。
- 拍照模式里对巨人摆姿势 + 与城市同框截图。
- 保存的"姿势卡"可让巨人随机取用，等于给巨人**无限的动作库**（省掉做 VMD 的成本）。

### 7.5.8 内容数据化（给实现者的架构建议）

- 建筑预设、街具清单、道具、成就、模式参数、性格参数**全部走配置表**，
  不要散落在代码里。加内容 = 加一行配置，而不是改逻辑。
- 内容做成**分级开关**（低/中/高），低端机自动降级 —— 与 §11 性能预算配合。

### 7.5.9 内容优先级总表（照这个顺序做，别并行铺开）

| 级别 | 内容 | 说明 |
|---|---|---|
| **P0 必须** | 生存模式、自由沙盒模式、街区+街具+车辆、NPC 行人、闪避翻滚、无敌开关、结算与最佳成绩 | 缺任何一条都算"不成型" |
| **P1 应该** | 巨人性格 + 动作库、藏身与搜索（潜行）、道具 5 种、公园/工地/水岸分区、地标、夜晚档、音效与震动、局内播报、成就、巨人模式下踩 NPC | 决定"耐不耐玩" |
| **P2 加分** | 挑战关卡、拍照/摆姿模式、天气、慢动作特写、收集解锁、地面水洼涂鸦等细节 | 决定"惊艳不惊艳" |

> 给实现者的话：**不要一口气全铺开。** 先把 P0 做扎实并真机验证，
> 再按 P1 逐项加，每加一项都能单独截图演示。内容量不是难点，
> **能稳定叠加而不崩，才是这个项目真正的难点。**

---

## 8. 模型导入（修"导入不了 PMX"）

### 8.1 检查清单
1. **必须走 MMD 管线**：`mmdRuntime.createMmdModel(mesh)`，否则是普通网格，无 MMD 材质/物理/IK（表现就是灰模或不可见）。
2. **贴图路径**：PMX 引用的贴图必须**与 PMX 同目录**（子目录同理需保持相对结构），否则材质丢失。
3. **加载器注册顺序**：材质构建器 → PMX loader → BMP loader，顺序错了会静默失败（现有工程注释里已记录此坑，务必保留）。
4. **文件访问**：Android 侧通过 SAF 授权目录，前端经本地代理 URL 读取；确认中文文件名/空格已正确编码。
5. **失败必须报错**：把加载异常、缺失贴图数量、骨骼数、模型包围盒尺寸**显示在界面上**，不要静默回退成默认模型。
6. **归一化**：导入后自动——按包围盒把模型脚底对齐地面（`y=0`）、朝向统一（面向 -Z 或 +Z 二者择一，与场景约定一致）、记录身高供 §3 尺度系统使用。

### 8.2 验收
- 用**至少 2 个不同 PMX**（例如昔涟 + 流萤）分别在真机验证：能导入、有材质、站在地上不悬空不陷地、尺寸符合预期。
- 故意放一个贴图缺失的模型，界面要给出明确提示而不是黑屏。

---

## 9. UI / HUD 规范（修"按钮太小"，横屏）

### 9.1 尺寸硬标准
- 所有可点元素**最小命中区 48×48 dp**（≈ 96~120 物理像素 @devicePixelRatio 2~2.5）。
- 主操作按钮（退出/重启/切换视角/无敌）建议 **64~80 dp**。
- 元素间距 ≥ 12 dp，避免误触。
- 按钮用**图标 + 文字**双保险（图标要能一眼看懂）。

### 9.2 横屏布局（示意）
```
┌──────────────────────────────────────────────────────────┐
│ [退出] [重开]        ⏱ 存活 42s       [无敌] [设置] [视角] │  ← 顶栏，高 ≥ 56dp
│                                                          │
│                        （游戏画面）                        │
│                                                          │
│  ╭──────╮                                                │
│  │ 摇杆 │                                       ﹝角落小地图﹞│  ← 左下摇杆 Φ≥140dp
│  ╰──────╯                                                │
└──────────────────────────────────────────────────────────┘
```
- 左下：移动摇杆（Φ 140~160 dp，半透明）。
- 右下：视角滑动区（其余大片区域），**不与摇杆重叠**。
- 右上/左上：状态与按钮。
- 设置抽屉从侧边滑出，占屏宽 ≤ 360 dp，滑杆要粗（轨道 ≥ 6 dp，滑块 ≥ 24 dp）。

### 9.3 视觉
- 半透明深色玻璃底 + 细边框，文字保证在亮/暗场景都清楚（加描边或阴影）。
- 状态提示（"被踩到了！"）用**大字 + 短时**，别用小字长句。
- 竖屏时如果没锁横屏，至少要有"请横屏使用"的提示页（但方案建议直接锁横屏）。

---

## 10. 架构重构（把"游戏"和"模型查看器"彻底分开）

现有代码把踩城逻辑塞在 `main.js`（近 3000 行，含 MMD 播放/AI管家/导出/摆姿势/踩城五套功能）里，这是"改不动"的根源。

**目标结构**（同名文件按需拆分，不强制 TypeScript，但模块边界必须清晰）：

```
src/
  core/        引擎/场景/共享工具（尺度、射线、程序化贴图、实例化池）
  mmd/         MMD 相关（模型加载、VMD 播放）—— 基本不动
  game/
    CityKit      城市生成（建筑/路面/街具/绿化/天空/光照）
    Player       玩家控制器（移动/碰撞/角色）
    CameraRig    相机（FPS/TPS/观察 + 碰撞 + 灵敏度）
    GiantAI      巨人状态机 + 踩踏判定 + 演出
    HUD          横屏 UI + 设置面板
    GameMode     组合以上，提供 enter()/exit()/restart()
  shells/      原有功能（预览/摆姿势/导出/AI管家）
```

- `GameMode.enter()` 负责：隐藏其它 UI、切换相机、生成城市、放置玩家与巨人、挂 observer。
- `GameMode.exit()` 负责：**逐块 dispose 并还原**（相机、城市网格、粒子、shadow generator、输入监听）。
- 所有模块自持 `CONFIG` 引用，禁止跨模块直接改状态。

---

## 11. 性能预算（红米 K90 / 骁龙 8 Elite，目标 60 FPS）

| 项目 | 预算 |
|---|---|
| 目标帧率 | 60 FPS（低电量档允许 30） |
| Draw Call | ≤ 300（靠实例化/合并） |
| 三角面 | 城市场景 ≤ 40 万（巨人 PMX 另计） |
| 动态光源 | 1 个投影平行光 + 少量非投影补光；路灯用自发光不投影 |
| 阴影贴图 | 2048² 以内，级联或收紧视锥 |
| 贴图 | 单张 ≤ 512²，总显存受控 |
| 粒子 | 扬尘 ≤ 200 个同屏 |

**超预算时的取舍顺序**：砍远景楼群数量 → 砍街道家具种类 → 降阴影分辨率 → 降粒子 → 最后才降建筑细节。
（即：**先保近处细节和巨人阴影**，这是观感的核心。）

---

## 12. 分阶段实施 + 验收（每阶段产出**可玩可验证**版本）

> 原则：每阶段结束都能在真机上跑、能截图判断对错。禁止把问题攒到最后一起调。

### 阶段 0：地基（不写玩法）
- 锁横屏、安全区、HUD 骨架（大按钮）。
- 尺度系统 + 出生点校验 + 调试读数（速度/坐标/FPS/相机模式）。
- **验收**：进入游戏能看到地面、能稳定站在一个安全点、HUD 按钮够大能点中。

### 阶段 1：相机 + 移动（"能玩"的门槛）
- FPS/TPS/观察三档 + 切换按钮；相机碰撞（不穿模）；设置面板（灵敏度等）。
- 玩家移动 + 摇杆（与视角区不重叠）+ 加速插值。
- **验收**：能自由跑动、视角不穿地不穿楼、灵敏度滑杆实时生效。

### 阶段 2：城市（"精致"）
- CityKit 全量：建筑生成器 → 路面/人行道 → 街具 → 绿化 → 天空/雾 → 光照阴影 → 时间档。
- **验收**：§4.9 的 8 条全部打勾。

### 阶段 3：巨人 + 玩法闭环（P0 内容）
- GiantAI 状态机、追踩、步态不出戏、踩踏判定与演出、免伤、结算、重开。
- 无敌开关；**生存模式 + 自由沙盒模式**双模式切换。
- NPC 行人 + 可被踩扁的车辆（尺度感演示）。
- **验收**：能玩满一局并结算；无敌下反复被踩不判负；退出后回到原界面无残留。

### 阶段 4：内容加料（P1，逐项独立提交，每项可单独演示）
- 巨人性格 + 动作库；藏身与搜索（潜行）；道具 5 种。
- 公园/工地/水岸分区 + 地标；夜晚时间档。
- 音效与震动；局内播报；成就；**巨人模式（操控巨人踩 NPC）**。
- **验收**：每加一项都能在真机上单独跑通并截图；连续叠加不崩、帧率不掉出预算。

### 阶段 5：打磨与加成内容（P2）
- 挑战关卡、拍照/摆姿联动、天气、慢动作特写、收集解锁、地面水洼/涂鸦等细节。
- 性能压测与降级策略；连续进出游戏模式 10 次不崩不残留。
- **验收**：真机 60 FPS 稳定；内容分级（低/中/高）开关有效。

---

## 13. 风险与注意事项

1. **别真放大 PMX**：物理裙摆/IK/VMD 全是原生尺度，一缩放全崩。永远用"玩家 1.6 m + 城市米制"来做巨人感。
2. **别把速度写成常数**：一切从身高推导，否则换模型/换尺度就失灵。
3. **相机穿模**是这类玩法的头号体验杀手，必须做碰撞 + 地面钳制。
4. **触控区域重叠**会让"移动不了"看起来像引擎 bug，先划分区域再排查逻辑。
5. **程序化贴图要缓存复用**，不要每个物体生成一张（会爆内存 + 卡顿）。
6. **阴影别贪**：一个投影平行光足够，路灯靠自发光；多个投影光源直接掉一半帧。
7. **退出清理**要写全，否则第二次进入会残留城市/相机/监听，表现为"越来越卡"。
8. 中文文件名（模型/贴图在中国用户环境很常见）的编码问题要在导入管线里统一处理。

---

## 14. 交付要求（给实现者）

1. 按 §12 分阶段提交，每阶段可独立运行、可截图验证。
2. `CONFIG` 集中所有可调数值，并在文档里列一张"参数表"（名/默认值/范围/作用）。
3. HUD 内保留一个**调试开关**（显示 FPS / DrawCall / 坐标 / 状态机状态 / 相机模式），便于验收。
4. 提交说明里写清：本阶段解决了 §1 表里的哪几条、如何验证。
5. 完成标准 = **§1 的 10 条全部解决** + §12 各阶段验收通过 + **§7.5.9 的 P0 内容全做齐** + 真机 60 FPS。
6. 内容按 §7.5.9 的优先级推进，**不要并行铺开**；每项 P1/P2 内容都要能单独跑通并截图演示。

---

## 附：本方案与旧版的关键差异（一眼看懂改了什么）

| 维度 | 旧版（v1.2） | 新版（v2.0） |
|---|---|---|
| 屏幕 | 竖屏 | **锁横屏**，HUD 重排 |
| 尺度 | 无系统，眼高写死 5cm、速度 0.10 | **以玩家 1.6m 为基准推导一切** |
| 视角 | 单一第一人称（埋进地里） | **FPS / TPS / 观察 三档 + 相机碰撞** |
| 灵敏度 | 单一倍率 | **水平/垂直独立 + 反转 + 阻尼 + FOV + 机位距离** |
| 城市 | 一块地 + 纯色盒子 | **模块化建筑生成器 + 路面/街具/绿化/天空/雾/阴影/时间档** |
| 建筑 | 无窗无顶 | 退台轮廓 + 程序化窗格(部分点亮) + 屋顶道具 + 底座商铺 |
| 光照 | 基础 | 投影太阳光 + 巨人巨大阴影 + 接触阴影 + 夜景自发光 |
| 输入 | 未接通/被遮挡 | **区域划分 + 调试读数 + 加速插值 + 奔跑/翻滚** |
| 玩法 | 状态机边界模糊、无结算闭环 | **时间驱动状态机 + 踩踏判定 + 免伤 + 结算 + 重开** |
| 内容量 | 单一循环（被追着踩，活 60 秒） | **5 种模式 + 分区城市 + 道具/藏身/潜行 + 巨人性格与动作库 + 成就/播报/特写 + 天气与夜晚**（见 §7.5） |
| 代码 | 全部塞在 main.js | **game/ 五模块独立 init/dispose** |
| 性能 | 无预算 | **60FPS 预算表 + 超预算取舍顺序** |

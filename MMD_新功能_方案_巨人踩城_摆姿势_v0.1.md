# MMD 项目新功能方案 —— 「巨人踩城」玩法 + 「手办式全身摆姿」

> 用途：把这份文档整体交给另一个 AI（或接手的开发者），让它据此在你的
> **babylon-mmd 网页前端**（`mmd-frontend-src`，PMX 模型 + VMD 动作 + Bullet 裙摆物理）
> 上实现两个新功能。所有决策、口径、验收标准都已写清，接手方可直接照做。
> 若你手头代码结构与本文描述不符，以你实际仓库为准，但请保留"模块/开关"的划分思路。

---

## 〇、一句话需求（可直接粘给别的 AI）

> 在我已有的 babylon-mmd 网页（加载 PMX 巨人模型 + VMD）基础上，增加两个互不干扰、可独立开关的功能模块：
>
> **模块 A「巨人踩城」**：进入一个第三人称城市街区沙盒，把已导入的 PMX 模型当作"巨人"，
> 让它在街道上**自然行走并追着小人踩**；玩家操控一个"小人"在楼房间逃跑/躲避，被踩到即失败，
> 但提供**无敌模式**开关（开启后踩不死）。场景含低多边形楼房、马路、路灯、树等城市要素。
>
> **模块 B「手办摆姿」**：像手机上的模型摆姿工具（DIY 手办模特/摆姿势类 App）那样，
> 进入后可**单独控制人体模型的每一个骨骼关节**（含手指每节、脚踝、手腕、头、腰、腿等一切可动骨），
> 操作直观顺手：点选部位 → 用旋转圈 / 单轴滑杆把关节转到想要的角度，可保存/调用多套姿势。

两个模块建议做成顶栏模式切换（正常播放 VMD 舞蹈 / 巨人踩城 / 摆姿），互不打架。

---

## 一、需求背景（接手者需理解的项目现状）

- 技术栈：纯网页（手机浏览器/内嵌 WebView）。`@babylonjs/core` + `babylon-mmd`（MMD 运行时，
  自带 Bullet WASM 裙摆物理、IK、PMX/VMD 加载）。
- 当前主流程：加载 PMX → 创建 `MmdRuntime`/物理 → 加载一个 VMD → `mmdModel.setRuntimeAnimation` 播放。
- 已有可复用的东西：PMX 模型加载、VMD 加载、物理裙摆、IK 由 babylon-mmd 自带处理。
- 关键：现有模型按 PMX 原生尺度（人形，约 1.6m 量级）渲染。做"巨人"不必真放大模型，
  **把"小人 + 街道场景"整体缩小**到模型脚底附近即可，巨人在视觉上自然成为巨人（见 §4.1）。
- 相机目前是 `ArcRotateCamera` 轨道相机，围绕模型。新玩法需要能切换成第三人称跟随相机。

---

## 二、模块 A：巨人踩城玩法

### A.0 玩法定义（先读，避免做偏）

- 场景舞台 = 一小块**城市街区**（含马路、楼、路灯、树），是"小人"的活动场地。
- 你的 PMX 巨人模型被放置在场地上，**由程序驱动**在街区里巡游，并**锁定并追赶玩家小人**。
- 玩家 = 屏幕上一个小的可操控角色（默认第三人称跟在玩家后面拍，能看到被追的紧张感）。
- 目标：在被巨人踩到 / 追上之前**存活尽量久**（可设 30/60 秒目标）。
- **无敌模式**：一个显眼开关，开启后玩家被踩到也不会死——只触发"被震开/角色踉跄"的演出效果。
- 额外加分（可选做）：街区里撒十几个"路人小人"NPC，巨人会顺带把它们踩成贴地的"纸片"，
  制造沙盒趣味；玩家也可选择把自己调成观察视角看巨人踩 NPC（属于"观赏模式"扩展）。

> 口径提醒：需求原话"要追着小人踩（玩家扮演小人）"里的"小人"= **玩家扮演的逃生者**。
> 被追着踩的对象是玩家，不是巨人踩一片随机的"小人"。上面"A.0 加分项"里的 NPC 路人只是锦上添花。

### A.1 玩法与规则
1. 玩法循环：开场提示"躲避巨人的脚步，存活 60 秒" → 巨人开始在街区追踩 → 计时/计分 → 结束结算。
2. 失败判定：巨人脚掌中心球体与玩家身体碰撞体积相交，且那一刻判定"踩踏命中"（见 A.4）→ 玩家被"踩扁/压扁"演出 → 失败。
3. 无敌模式开：命中不判失败，仅播放"角色被弹飞/踉跄"的演出，并短时间（如 2 秒）免伤，避免被连踩。
4. 玩家可随时重开（R 键 / 界面按钮）。
5. 建议难度渐变：随存活时间，巨人逼近速度 / 仇恨锁定稍加强。

### A.2 玩家（"小人"）控制 —— 手感
- 移动：WASD（桌面测试）+ **移动端虚拟摇杆**（左下角圆盘，原生、灵敏）；相机相对方向移动（角色朝镜头输入方向跑）。
- 操作项：跑（按住 Shift/摇杆外圈）、**闪避翻滚**（空格/摇杆旁按钮，短时快速位移+短无敌帧）——这是"躲避踩踏"最吃手感的一环，务必做得跟手。
- 玩家小人用什么模型：优先复用一套**已有的轻量人形**（可以是同款 PMX 缩小，或 babylon 内置的 capsule 小人/简单角色）。为避免"巨人脚下一个光杆胶囊"太丑，默认做一个带颜色的小小角色（手/脚可见），并允许后续替换成任意 PMX。
- 玩家的身高与巨人比例：让玩家小人站到巨人脚踝旁时约为脚踝高度（视觉上明显是小人），比例可调（见 A.4 参数表）。

### A.3 巨人行为（"自然"是核心要求）
难点在"动作要自然"。给接手的 AI 三条递进实现路线，从易到难，**先做 1，能跑通再上 2/3**：

1. **位移 + 一条"步态/行走"VMD 或程序步态**：
   - 巨人根骨（MMD 里的 センター / 全ての親）每帧做**水平移动**（朝目标/玩家方向），并做朝向旋转。
   - 让它**面向移动方向**，配一条正向行走的 VMD 动作（原地循环），或程序式抬腿摆动（左右脚交替抬起/落地）。
   - 关键技巧：移动速度必须与步态频率匹配，脚底才不会"飘/滑冰"——脚步挪动频率要跟上平移速度。
2. **脚步落点朝向玩家**（"追着踩"的雏形）：
   - 每当巨人走到玩家附近一定距离，触发一次"踩踏意图"：把**准备落下的那只脚的落点瞄准玩家当前位置**，
     配合身体前倾、抬脚再用力踩下。落点预测见 A.4。
   - 这一步需要能控制单腿（右足/左足/右足首等骨），建议暂时关闭该腿的 IK 由程序直接摆，或先用现成"踩踏 VMD"。
3. **分层状态机 + 自然过渡**（想要"真自然"就靠它）：
   - 状态：巡游(loiter) → 发现(spot/锁定) → 追捕(chase) → 抬脚准备踩(stomp-windup) → 踩下(stomp) → 收脚回巡游。
   - 状态间用动作混合/过渡（不是硬切），追捕时头部/视线朝向玩家（可动 頭 骨轻微下压看向脚下的小人，非常出效果）。
   - 若用户提供一个或多个踩踏/步伐 VMD，直接套在对应状态上最省事；否则用程序骨控顶替。

- **"追着踩"命中逻辑（A.4）——碰撞与演出**
  - 把巨人**左右脚掌**各挂一个碰撞体（随 足首/趾 骨世界位更新），半径 R（可调，参数表有默认）。
  - 判定分两段：①抬脚后脚掌处于"下落中"（y 递减）②此时玩家中心是否在脚掌圆内。两条件同时满足 → 命中。
  - 命中演出：巨人脚掌压到地面 → 地面扬尘/震动（简单粒子或屏幕抖动）+ 玩家被"踩扁"贴地（若无敌，改为弹开+踉跄 + 镜头晃动）。避免出现"玩家被踩但穿模/脚悬空"的穿帮：命中瞬间把玩家拉到脚掌下方再压。
  - 防连踩：命中后给玩家 1.5~2s 无敌帧。

### A.4 参数表（都给默认值，方便接手方先跑通再调）
| 参数 | 默认 | 说明 |
|---|---|---|
| 玩家移动速度 | 5.0 | 单位/秒 |
| 闪避翻滚速度/距离 | 12 / 3 | 翻滚爆发 |
| 巨人移动速度 | 2.8 | 追逐时可提到 3.4 |
| 追踪发现半径 | 25 | 进入后转为"追捕" |
| 踩踏触发距离 | 4.5 | 到达此距离进入"抬脚准备" |
| 脚掌碰撞半径 R | 1.6 | 每只脚 |
| 玩家命中无敌帧 | 2.0s | |
| 存活目标 | 60s | |
| 玩家/世界缩放 | 0.1（相对模型）| 让模型显得是巨人 |

### A.5 城市街区场景（模块 A 的地图）
没有现成城市美术，**用程序生成的极简低多边形(白模+简单色)即可**，不要引入外部模型依赖。
- 地面：一块大马路地面（横纵主路 + 人行道），贴简单纯色/色带。
- 楼房：随机高度/宽度的长方体（低多边），带窗户贴图或镂空简单感即可；布置在马路两侧留出中间街道。
- 路灯、树：路灯=细杆+灯头，树=圆柱树干+球体/锥体树冠（球体随机绿）。沿路间隔放置。
- 用 babylon 的 `MeshBuilder`/`CreateGround` 即可，全部 procedural，无美术资源。
- 给玩家留一条"逃窜的街道"（有岔路、可绕楼），别做成死胡同到不了头的空广场。
- 城市区块要**避开巨人出生/行走路线**留出空地，也避免巨人撞穿楼——要么楼在路边不挡路径，要么巨人对楼做简单"绕过/撞毁"（撞毁先不做，绕行即可）。

### A.6 相机
- 默认**第三人称跟随相机**：跟随玩家小人，镜头稍高（俯视地面能看到巨人逼近 + 玩家脚下），能看清"大脚从天而降"的压迫感。
- 支持切到"远观"视角（看巨人整体）用于观赏。
- 移动端单指 = 移动；双指可小幅调整相机环绕/距离（不强制，有更好）。

---

## 三、模块 B：手办式全身摆姿

### B.0 参考（已替你调研，接手者直接用交互范式即可）
"单独抠每个关节、包括手指每节，且要方便顺手"的做法，现有手机摆姿工具普遍是这套交互范式（可在文档里引作参考，不必写死某个 App 名）：
- **点选身体部位选中骨骼** → 身上出现**旋转操作圈（gizmo）** → 在圈上拖动/单轴滑杆精确转骨。
- 辅以**骨骼列表/层级树**（按身体分组：头/躯干/左右臂/左右手/左右腿/左右脚…逐根骨可点）。
- **FK 链**：掰手指末节时，从该骨往后（向根）整条链联动，或用"仅本骨旋转"两种模式切换。
- **对称镜像**：调好左手姿势可一键复制到右手。
- **预设姿势保存/读取**（多槽位）+ 单骨复位 / 全身复位。

### B.1 功能需求
1. **进入摆姿模式**：关掉/冻结 VMD 自动播放与裙摆物理（避免物理跟手打架、模型乱晃），模型停在 T-pose 或当前姿势可编辑。
2. **骨骼全覆盖**：能访问并旋转该 PMX **全部可动骨骼**，至少包括并正确分组：
   - 头颈：首、頭、両目/眉（有则）
   - 躯干：上半身、上半身2、下半身、腰（センター/腰キャンセル 根骨只做整体位移不旋转）
   - 上肢：肩、腕、肘(ひじ)、手首(腕)、各手指骨（拇指/食指/中指/无名指/小指，每指从掌到尖的 2~4 节：親指１…３、人差指１…３ 等）
   - 下肢：腰・臀、腿(もも)、膝(ひざ)、足首(脚踝)、足趾
   - 脚踝=足首、手踝(手腕)=手首、腰=上半身/下半身，命名按 MMD 日文骨名（模型自带），UI 显示成中文便于操作。
3. **两种精确控制并存**（这是"顺手"的关键）：
   - 圈控：选中骨后显示以该骨为圆心的旋转圆环（X/Y/Z 三色环），拖某环 = 只绕该轴转，角度实时显示。
   - 微调滑杆：当前骨给出 Pitch/Yaw/Roll 三根滑杆（−180°~180°），可精确到 0.5°/0.1°，用于定型。
4. **FK / 手动链**：默认"只转本骨"；提供"联动其后代"（握拳/收整条手臂自然）的按钮切换。
5. **镜像复制**：一键把某一侧（左/右）骨骼姿势复制到对侧并镜像。复制也分组（如只镜像手、或整身）。
6. **快捷与手感**：
   - 旋转值随拖动实时更新到画面（所见即所得）。
   - 选中骨骼高亮、骨骼树里同步定位；点骨骼树项 = 选中对应身体部位。
   - 撤销(ctrl/按钮)/重置当前骨/重置全部。
   - 手机竖/横屏都可用，UI 不遮挡模型主体。
7. **姿势存档**：至少能保存/加载 8~12 套姿势（JSON 存每骨旋转/平移），重启保留；可选导出为可直接复用的人体姿势数据。
8. 摆姿结束切回"播放 VMD"时：先复位/清掉手动姿势，再正常播动作（避免残留扭曲）。

### B.2 实现要点（给接手者的工程提示）
- 摆姿=**逐帧把目标骨的局部旋转写进模型每帧变换**，而不是依赖 VMD。需确认 babylon-mmd 里骨/物理/IK 的更新顺序，
  摆姿时必须**暂停物理与目标链 IK**，否则每帧会被 IK/物理回拉，手一松姿势就弹回。这是最容易踩的坑，优先解决"姿势能稳定停住"再谈手感。
- 绕轴旋转用四元数累乘到该骨当前局部旋转即可；注意 MMD 骨旋转的欧拉顺序/轴语义以实际试出来为准（不同模型细节不同），先拿一个模型校准坐标系方向。
- 选中骨：用射线拾取身体网格表面 → 换算到最近的骨，或直接点模型上可见的"骨点"。简化起见第一版可用"骨骼树列表点选 + 画面点击身体部位"两种方式。
- 手指骨细且密集，圈控容易误触：给指骨提供"放大 / 高对比手柄"选项，或干脆对单根手指也走滑杆微调最稳。
- 摆姿与模块 A 共用"模型"：A 里玩家小人 + 巨人，B 里对当前选中的任意已加载模型都能摆（默认对巨人摆，也可切到小人）。

---

## 四、整体架构与实施建议（给接手者）

### 4.1 用缩放做"巨人"（强烈建议，别真放大模型）
- 方案：场景世界保持 PMX 原生比例，只把"小人 + 城市街区"整体放进一个小比例组（约 ×0.1，见 A.4）。
  巨人仍在 (0,0,0) 附近、直立；玩家小人/城市围绕它布置，视觉即成"城市+巨人"。
- 好处：裙摆物理、IK、VMD 全都不用改尺度，白赚"巨人感"。
- 若反过来把模型放大 10 倍，物理裙摆/相机/IK 全要重调，坑多。**别这么做。**

### 4.2 模块划分（建议按此组织代码）
- 顶栏 3 个模式：`[播放 VMD] [巨人踩城] [摆姿]`。
- 每个模式独立初始化/销毁，互相切换时干净卸载，别共享脏状态：
  - `StompGame.ts`：城市街区生成、玩家角色、巨人 AI、碰撞判定、相机、HUD/结算。
  - `PoseEditor.ts`：骨骼 UI(树/圈/滑杆)、FK、镜像、姿势存档。
  - 底层公用的"每帧驱动骨旋转"与"暂停物理/IK"封装成一个共享工具函数，两个模块都用。
- 所有可调数值放进一个 `CONFIG` 常量对象（含 A.4 参数表），方便调参不改逻辑。

### 4.3 里程碑（由易到难，先各模块有个能玩的竖版）
- **M1（踩城竖版）**：城市 + 玩家 WASD/摇杆跑动 + 巨人直走巡游（路线 1 的最简单步态）→ 能躲。
- **M2**：加入"追 + 瞄准落点踩"（A.3 路线 2）+ 命中/失败/重开。
- **M3**：无敌模式开关 + 踩踏演出(扬尘/抖动/踉跄) + NPC 路人被踩成纸片(加分)。
- **M4（摆姿竖版）**：骨骼树选中 + 单轴滑杆精确转 + 姿势稳定停住（先解决物理/IK 不打架）→ 能摆腿摆臂。
- **M5**：手部/指骨逐节控制 + 旋转圈 gizmo + FK 联动。
- **M6**：镜像复制 + 姿势存档(JSON) + UI 手感打磨(撤销/复位/高对比手柄)。
- 每个里程碑结束都留一个**可在手机真机验证**的入口，别拖到最后一起测。

### 4.4 验收标准（完成 = 满足这些）
1. 手机上能进"巨人踩城"，摇杆/键控顺畅跑，巨人自然追并踩，被踩有明确反馈；开无敌后反复被踩也不判失败。
2. 城市街区看起来像一条有楼/路灯/树的街，玩家能绕楼跑。
3. "摆姿"里能单独转动手指每一节、脚踝、手腕、腰、头等，松开手姿势不弹回；能存/读姿势。
4. 两个模式都可干净退出回到正常 VMD 播放，互不残留。
5. 不掉帧明显（红米 K90 / 8 Elite 需流畅；必要时降分辨率/简化树与楼）。

### 4.5 已知风险（提前知会）
- "动作自然"最难，尤其"巨人是人形但要走得像巨人压路"。请优先保证**脚与地面不穿帮、步频配速度**；不要一上来就堆 IK/物理。
- 暂停/恢复物理与 IK 的时机若没处理好，摆姿或踩城会有手一松姿势弹回 / 模型乱抖。
- 指骨多，UI 点选易误触，设计要宽容（放大命中 + 支持滑杆）。
- 别引入任何外部模型/贴图依赖——城市、小人、纸片演出都应能程序生成。

---

## 五、待你（用户）拍板的小决策（不影响接手者先开工主干）
1. **A.0 里"玩家就是被踩的小人"是否唯一玩法？** 文档已按此默认实现。若你更想"操控巨人去踩满街小人"，
   告诉我，我把主玩法对调（巨人变玩家可操控，满街 NPC 小人可踩成纸片）——两条线底层(A.2/A.3 的骨控与 A.4 判定)高度复用，切换成本低。
2. **玩家小人模型**：先用内置小角色，还是想我这边也换成某个现成 PMX 缩小？给个路径即可。
3. **是否要 NPC 路人小人**（被巨人踩成纸片，观赏性），做/不做？
4. 摆姿的**对象**：默认 = 已导入的巨人 PMX；是否需要也能切到玩家小人/任意已加载模型？

# 巨人踩城 · 制作文档 v1.0

> 配套 `01_需求文档_巨人踩城_v1.0.md`（**需求以那份为准，本文只讲怎么做**）。
> 目标仓库：`giant-step-caixiao`（Babylon + babylon-mmd + WASM Bullet 物理 + Android WebView）
> 本文只定义技术口径、坑位、验收方式与交付纪律，**不写具体代码**（代码在仓库里）。

---

## 0. 三条工程铁律

1. **永远不要缩放模型来做巨人感。** 缩放会让 MMD 的裙摆物理崩坏 —— 弹簧参数是按原尺度标定的，一缩放就脱离工作区间，**衣服当场乱飞**。
   → 正确做法：**缩世界**（§1）。
2. **禁止魔法数。** 一切从"玩家身高"推导（眼高、速度、半径、相机距离、粒子尺寸、音效距离）。
   换模型、改比例、调手感时，只改一个数。
3. **进入/退出玩法必须零残留。** 退出后回到原 MMD 播放界面要跟没进过一样；
   所有临时对象（城市网格、粒子、贴图、监听器、相机、光照）都要能被干净销毁。**连续进出 10 次不崩、不涨内存。**

---

## 1. 尺度系统（**先定这个，否则其余全是错的**）

### 1.1 三个层级

| 层级 | 含义 | 值 |
|---|---|---|
| **世界单位** | 全部代码统一用米 | 1 = 1 m |
| **巨人** | 加载的 PMX，**原生尺度，不缩放** | 体感身高 ≈ 30 m |
| **玩家小人** | 明确设定身高 | **1.6~1.7 m** |
| **比例** | 巨人 / 玩家 | **≈ 18~20 : 1** |

> 实现建议：把巨人按原尺度放好，**把玩家、城市、街具按"真实米制"做好，再整体缩小世界**。
> 无论怎么实现，**代码里必须以"玩家身高"为基准推导一切**，而不是"乘以 0.05"。

### 1.2 硬性要求

- **禁止写死相机眼高 / 移动速度**。必须推导：
  - 第一人称眼高 = `身高 * 0.92`
  - 碰撞半径 = `身高 * 0.25`，高 = `身高`
  - 行走速度 = `身高 * 0.82 /秒`（≈1.4 m/s 的体感），奔跑 = ×1.8
  - 翻滚冲刺 = ×3 左右，带 0.3~0.5 s 无敌帧
- **出生点必须校验**（这是"为什么一进游戏就卡在楼里"的根治手段）：
  向下射线确认脚下有地面 + 周围球形检测确认不在几何体内 + 头顶净空 ≥ 2×身高；
  不满足就换点重试（最多 20 次），最后兜底到"中央广场预设安全点"。
- 相机 near clip 要按世界尺度收紧（避免与头发/衣服穿插），far 按城市可视距离 + 远景层。

### 1.3 验收

- 第一帧能看见城市与地面。
- 小人头顶大概到她**脚踝**。
- 连续重开 20 次，无一次卡在几何体内。

---

## 2. 相机系统

### 2.1 三档机位（一个 `CameraRig` 统一管理）

| 模式 | 说明 |
|---|---|
| **FPS 第一人称** | 眼高 = `身高*0.92`；抬头能看到她全身（**这玩法最爽的视角**） |
| **TPS 第三人称** | 弹簧臂跟在后上方，距离可调；能看见自己的小人 |
| **观察 / 自由** | 环绕轨道，用来看她全身；也用于拍照/摆姿 |

- 一个**大按钮**循环切换；进入玩法默认 **FPS**。
- **不要**建多台相机来回启用（会带来三份状态：听众、深度、后处理）。

### 2.2 相机碰撞（**直接解决"视角穿模"**）

- TPS：从角色头部向目标机位做**射线检测**，命中几何体就把相机拉到命中点之前（留余量）。
- FPS：角色自身网格不参与相机射线判定（否则会被自己顶住）。
- **任何情况下相机不得低于地面**：每帧一次向下射线，把相机钳到地面之上。

### 2.3 眩晕与手感

- 俯仰限位 ±85°，不翻转。
- 默认开一点阻尼/平滑（跟手但不生硬）。
- 移动时轻微 FOV 变化（可选，1~2 度）增加速度感。

### 2.4 设置面板（"灵敏度要可调"）

全部**大滑杆 + 实时生效 + 本地持久化**：
水平灵敏度 / **垂直灵敏度（独立）** / 反转 Y 轴 / 视角阻尼 / FOV / TPS 距离 / 肩部偏移 / 摇杆灵敏度 / **震动强度**。

> 灵敏度要与 FOV 联动换算，否则改 FOV 手感会变。

---

## 3. 玩家控制器与输入（"移动不了"的根治）

### 3.1 排查顺序（按此顺序，别跳）

1. **先放调试读数**（速度 / 坐标 / 状态 / 相机模式 / 输入原始值）—— 眼见为实。
2. **输入有没有真的到引擎**：渲染循环是否唯一、监听器是否挂上。
3. **速度是否按身高推导**（不是写死的常数）。
4. **触控区是否重叠**：这是这类实现最常见的"移动不了"原因 ——
   **触控区必须显式划分且不重叠**：左手摇杆区（屏幕左下 ~35% 宽）、右手视角拖动区（其余），中间留按钮区。
5. **角色朝向是否被某个旋转覆盖**：移动方向要按相机朝向计算，**只取水平分量**（别让角色往天上走）。

### 3.2 移动手感

- 加减速插值（不要瞬间启停）。
- 行走 / 奔跑两档。
- **闪避翻滚**（带无敌帧）—— 躲踩的核心动作，**P0**。
- TPS 下朝向平滑转向；FPS 下隐藏自身（或只留影子与脚下接触阴影）。

---

## 4. 城市生成（城市是玩具，不是布景）

> 硬要求：**不引入任何外部模型/贴图文件**（包体可控、无版权风险）。
> 全部用引擎的图元 + **程序化生成的贴图**（离屏绘制窗格/砖缝/沥青/标线，一次生成、全场景复用）。
> **精致 ≠ 面数多**，而是层次、重复中的变化、材质、光影。

### 4.1 建筑生成器（核心，不要"随机一个盒子"）

一栋楼 = **基座 + 标准层 + 顶部**，各自独立随机：

- **体量**：宽 8~20 m、进深 8~20 m、高 12~80 m；支持**退台**（上层内缩）与**双体量拼合**（轮廓不是单一立方体）。
- **立面**：程序化窗格贴图，**随机点亮 15~30% 的窗**（夜景关键）；加窗台线、腰线、竖向分隔条。
- **底座（1~2 层）**：商铺/大堂 —— 玻璃门 + 暖色内透光 + 招牌色块 + 遮阳棚，与上部立面明确区分。
- **顶部**：女儿墙 + 随机屋顶道具（水箱、空调外机、通风管、天线、电梯机房）。
- **变化来源**：分区色板（商业区冷灰蓝 / 住宅区暖米 / 老城区黄褐）× 窗格密度 × 退台层数 × 屋顶道具组合。
- **布局**：主街两侧各 6~10 栋，后排 1~2 圈做层次；**沿街立面必须贴合街道边界**（不能歪进马路）；栅格抢占式布局，保证没有楼压到路面。
- **远景层**：300~500 m 外一圈**低模剪影楼群**（无窗、单色、受雾影响）撑天际线。近处细节 + 远处剪影 = 城市感。

### 4.2 路面 / 人行道

- **车道**：沥青 + 程序化贴图（车道虚线/实线、磨损、油渍、补丁），主干道 2~4 车道。
- **人行道**：**抬高 0.15 m**（路缘石），用不同铺装格子贴图。这条"高差"是廉价但极有效的城市感来源。
- **路口**：十字/丁字 + 斑马线 + 停止线 + 转向箭头（贴花）。
- **附属**：井盖、雨水篦子、无障碍坡道。
- **地面不能是一块纯色平面**：至少 3 种材质分区（车道 / 人行道 / 广场砖）。

### 4.3 街道设施（决定精致度的关键，别省）

沿街**按固定间距自动布置**，每类都要有细节：
路灯（灯杆 + 弯臂 + 灯头，夜间灯头发光 + 地面光斑贴花）／行道树（树干 + 2~3 层树冠，或十字交叉的 alpha 叶片面片）／信号灯与路牌／长椅、垃圾桶、消防栓、公交站牌雨棚、花坛、护栏／老城区加电线杆 + 电线（下垂弧线，成本极低观感提升明显）／广告灯箱（自发光，夜景最出效果）。

> 这些不是装饰，是**玩法资源**：垃圾桶可藏身、售货机可推倒挡路、消防栓被踩会喷水柱、交通锥可以被踢飞。

### 4.4 绿化 / 天空 / 雾 / 时间档

- 绿化：行道树 + 街角灌木 + 草地花坛（不同色贴地材质）；可选极少量落叶粒子。
- 天空：渐变天空盒（顶部深蓝 → 地平线暖橙/淡青），按时间档切换。
- **雾必须开**（指数平方），密度让 300 m 外明显衰减 —— 既出纵深又遮远景接缝。
- **时间档**：白天 / 黄昏 / **夜晚**（**建议默认黄昏或夜晚**，这是巨人题材最出气氛的档）。
  切档要同时改：太阳角度与颜色、环境光、雾色、窗格点亮比例、自发光强度。

### 4.5 光照与阴影

- **一个平行光做太阳，必须开阴影**。**她投在街道和楼上的巨大阴影是"巨人感"的核心来源之一，优先级最高。**
- 半球光做环境光，避免暗部纯黑。
- **夜景补光用自发光 + 贴花，不要加实时光源**（多个投影光源直接掉一半帧）。
- **接触阴影**：玩家脚下加一个软圆贴花，消除"悬浮感"。

### 4.6 材质（不许纯色）

每个材质至少：基础色（贴图或分块色）+ 粗糙度（沥青粗糙、玻璃光滑）+ 金属度（金属街具）。
程序化贴图**一次生成、全场景复用并缓存**，**不要每个物体各画一张**（爆内存 + 卡顿）。

### 4.7 实例化与性能

- 重复物体（窗、楼、树、路灯、护栏段）**一律用实例化**，不要一个个新建网格。
- 远景楼群**合并网格**（1~2 个 draw call）。
- 贴图统一 256~512，开 mipmap。

### 4.8 「精致」的客观验收线（逐条打勾）

1. 站在街上看四周：能看到**至少 6 栋造型各异的楼**（不是同一盒子复制）。
2. 楼有**可辨认的窗格**，且**部分窗户亮着**。
3. 人行道与车道**有高差**，路面有**车道线/斑马线**。
4. 街上有**路灯、树、至少两种街道家具**。
5. 有**雾**，远处有**天际线剪影**，天空不是纯色。
6. **她的影子投在街上**，小人脚下有接触阴影。
7. 夜晚档能一眼看出"这是晚上"（窗光 + 路灯发光 + 整体色调）。
8. 截图不看 HUD，别人能认出"这是一条城市街道"，而不是"一堆盒子"。

---

## 5. 巨人系统

### 5.1 行为状态机（**时间驱动，不用帧数**）

`巡游 → 发现 → 追 → 逼近抬脚（蓄力）→ 踩下 → 收脚 → 巡游`，中间穿插 **搜索** 与 **日常**。

- 每个状态有 enter/exit + 最小持续时间 + 过渡时长，用时间累加。
- **追你时头要下压、视线跟着脚边的小人** —— 这是"追着踩"观感的关键。
- **发现判定** = 视线锥（角度 + 距离）+ 听觉（声音半径）。
- **搜索状态**：失去目标后不立刻重置，而是「原地张望 → 走向最后目击点 → 翻查藏身处」。

### 5.2 步态（**最容易出戏的地方**）

- 优先用现成的**行走 / 踩踏 VMD**。
- 没有合适 VMD 时，**用骨骼级程序控制**（直接驱动足部 IK 目标，让脚抬起来 / 往前挪），**不要自己去写骨骼旋转曲线**（会和 MMD 的 IK/约束求解打架）。
- **铁律：步频必须匹配平移速度**，否则脚底打滑，一眼假。

```
speed = 单步跨距 × 每秒步数
```

改速度时**先改步态节奏，再算速度**，不要让两者各自独立。

### 5.3 踩踏判定（可靠、自然、不过度）

- **判定体挂在脚掌**（跟随足部骨骼更新世界位置）。
- **两个条件同时成立才算命中**：① 该脚处于"踩下"且**持续下降**；② 玩家中心投影落在脚掌水平半径内。
- 命中给 **1.5~2 s 免伤**（避免一帧连续判定）。
- **必须有预备动作与延迟**（抬脚 → 停顿 → 踩下），让人"看得清、躲得开"，**禁止随机秒**。
- 判定时机要**排在骨骼求解之后**（骨骼最终位置是求解器写的；读早了就是慢半拍）。

### 5.4 演出

- 踩下与收脚的演出：扬尘（粒子，**同屏硬上限 200**）、地面碎屑、屏幕震动。
- 被踩中时给明确反馈（击退 / 重生到附近安全点），**不做强制 Game Over**。
- 音效要有方位感与由远及近的层次。
- 相机：她落地与逼近时给轻微的机位震动（幅度按世界尺度缩放）。

### 5.5 物理（自然性守则，用户明确要求"衣服不能乱飞"）

1. **不缩放模型**（§0 铁律一）。
2. 物理开关打开，确认地面碰撞存在（否则裙摆会与地面穿插）。
3. **物理子步数**是"裙摆顺不顺、容不容易穿模"最直接的旋钮：越大越稳越不容易穿，越费 CPU。
   → 默认别给太低；给用户一个滑杆，并在 UI 上标注"越高越顺、越费电"。
4. **物理时间步要稳**：不要让物理步长随帧率乱变；避免多帧跨步造成弹跳。
5. 若某个 PMX 的裙子本身刚体参数差（抖/飞），提供"布料强度/关闭物理"的调节；
   **但默认必须先把"不缩放 + 地面碰撞 + 子步数"这三件事做对**，不要用"关物理"掩盖问题。
6. **验收观感**：她走动/追你时，裙摆与头发自然跟随摆动 —— **不飘出体外、不撕裂、不原地乱颤**。

### 5.6 爬上她（技术要点）

她的**鞋带、鞋跟、裙摆、脚背**是可攀爬、可站立的位置（需求 §5.3.4）。
→ 这些位置全部由**骨骼世界坐标**驱动：站立、吸附、判定都要跟着骨骼走，
   不要用静态包围盒（她一动就会错位）。
→ 从上面摔下来要算一次明确的伤害/反馈。

## 6. 模型导入（"导入不了 PMX"的根治）

### 6.1 检查清单

1. **必须走 MMD 管线**加载，否则是普通网格 —— 没有 MMD 材质/物理/IK（表现就是灰模或不可见）。
2. **贴图路径**：PMX 引用的贴图必须**与 PMX 同目录**（子目录要保持相对结构），否则材质丢失。
3. **加载器注册顺序**：材质构建器 → PMX loader → 贴图 loader，顺序错了会静默失败。
4. **文件访问**：Android 侧经 SAF 授权目录读取；确认中文文件名/空格已正确编码。
5. **失败必须报错**：把异常文本、缺失贴图数量、骨骼数、模型包围盒尺寸**显示在界面上**，**不要静默回退成默认模型**。
6. **归一化**：导入后自动把脚底对齐地面、统一朝向、记录身高供尺度系统使用。

### 6.2 验收

- 用**至少 2 个不同 PMX** 分别在真机验证：能导入、有材质、站在地上不悬空不陷地、裙摆物理自然。
- 故意放一个贴图缺失的模型，界面要给出明确提示而不是黑屏。

---

## 7. UI / HUD

### 7.1 尺寸硬标准

- 所有可点元素最小命中区 **48×48 dp**；主操作按钮（退出/重开/切换视角/无敌）**64~80 dp**；间距 ≥ 12 dp。
- 按钮用**图标 + 文字**双保险。

### 7.2 横屏布局

```
┌──────────────────────────────────────────────────────────┐
│ [退出] [重开]        ⏱ 存活 42s      [无敌] [设置] [视角]  │ 顶栏 ≥ 56dp
│                                                          │
│                        （游戏画面）                        │
│                                                          │
│  ╭──────╮                                                │
│  │ 摇杆 │                                    ┌─────────┐ │
│  ╰──────╯                                    │ 动作按钮 │ │
└──────────────────────────────────────────────────────────┘
   ↑ 左下 Φ140~160dp      ↑ 右下：翻滚等动作
   视角拖动区 = 其余区域（与摇杆矩形不重叠）
```

- 处理刘海/挖孔安全区，HUD 元素避开。
- 设置抽屉从侧边滑出（宽 ≤ 360 dp），**滑杆要粗**（轨道 ≥ 6 dp，滑块 ≥ 24 dp）。

### 7.3 视觉

- 半透明深色玻璃底 + 细边框；文字加描边/阴影，亮暗场景都可读。
- 状态提示（「被踩到了！」）用**大字 + 短时**，别用小字长句。
- 未锁横屏时至少要有"请横屏使用"提示页（**但方案要求直接锁横屏**）。

---

## 8. 架构（把"游戏"和"模型查看器"彻底分开）

**现有问题**：玩法逻辑塞在近 3000 行的单个前端文件里（含 MMD 播放 / AI 管家 / 导出 / 摆姿势 / 踩城 五套功能），这是"改不动"的根源。

**目标结构**（模块边界必须清晰，模块名可不同）：

```
core/      引擎/场景/共享工具（尺度、射线、程序化贴图、实例化池、对象池）
mmd/       MMD 相关（模型加载、VMD 播放）—— 基本不动
game/
  CityKit     城市生成（建筑/路面/街具/绿化/天空/光照）
  Player      玩家控制器（移动/碰撞/角色）
  CameraRig   相机（FPS/TPS/观察 + 碰撞 + 灵敏度）
  GiantAI     她的状态机 + 踩踏判定 + 演出
  HUD         横屏 UI + 设置面板
  GameMode    组合以上，提供 enter() / exit() / restart()
shells/    原有功能（预览 / 摆姿势 / 导出 / AI 管家）
```

- `GameMode.enter()`：隐藏其它 UI → 生成城市 → **校验出生点**放玩家 → 加载她 → 切相机 → 关监听清理。
- `GameMode.exit()`：**逐块 dispose 并还原**（相机、城市网格、粒子、阴影、输入监听、光照、雾）。**退出后必须零残留。**
- 所有可调数值集中到一个 `CONFIG`；模块之间不直接改彼此状态。

---

## 9. 性能预算（Android，目标 60 FPS）

| 项目 | 预算 |
|---|---|
| 目标帧率 | 60 FPS（低电量档允许 30） |
| Draw call | ≤ 300（靠实例化/合并） |
| 三角面 | 城市场景 ≤ 40 万（她的 PMX 另计） |
| 动态光源 | 1 个投影平行光 + 少量非投影补光；路灯用自发光 |
| 阴影贴图 | 2048² 以内 |
| 贴图 | 单张 ≤ 512²，总显存受控 |
| 粒子 | 同屏 ≤ 200 |
| 物理子步 | 在"裙摆稳"和"不发烫"之间取平衡，**给用户可调** |

**超预算时的取舍顺序**：砍远景楼群 → 砍街道家具种类 → 降阴影分辨率 → 降粒子 → **最后才降建筑细节**。
（即：**先保近处细节和她的大阴影**。）

---

## 10. 分阶段实施（每阶段都要能真机跑、能截图判对错）

> 原则：**禁止把问题攒到最后一起调。**

### 阶段 0：地基（不写玩法）
- 锁横屏 + 安全区 + HUD 骨架（大按钮）。
- 尺度系统 + 出生点校验 + 调试读数（速度/坐标/FPS/相机模式）。
- **验收**：进游戏能看到地面；能稳定站在安全点；HUD 按钮够大能点中。

### 阶段 1：相机 + 移动（"能玩"的门槛）
- 三档机位 + 切换；相机碰撞与地面钳制；设置面板生效并持久化。
- 玩家移动 + 摇杆（与视角区不重叠）+ 加速插值 + 翻滚。
- **验收**：能自由跑动；视角不穿地不穿楼；灵敏度滑杆实时生效。

### 阶段 2：城市（"精致"）
- 建筑生成器 → 路面/人行道 → 街具 → 绿化 → 天空/雾/时间档 → 光照与阴影。
- **验收**：§4.8 的 8 条全部打勾。

### 阶段 3：她 + 玩法闭环（P0 内容）
- 状态机、追踩、步态不出戏、踩踏判定与免伤、演出、结算、重开。
- **自由沙盒模式（先做）+ 生存模式**。
- 视线锥 + 听觉（让她真的"看得见 / 听得见"，藏身才有意义）。
- **验收**：需求文档 §7 的"上手 30 秒 / 2 分钟"全部成立；退出后零残留。

### 阶段 4：内容加料（P1，逐项独立提交，每项可单独演示）
- 她的日常、搜索行为、换鞋、情绪、动作库、连锁破坏、NPC 出卖你、爬上她。
- 公园/工地/水岸 + 地标；夜晚档；心跳、慢动作特写、局内播报、音效全量；巨人模式踩 NPC。
- **验收**：每加一项都能单独跑通并截图；连续叠加不崩、帧率不出预算。

### 阶段 5：打磨与加成（P2）
- 拍照/摆姿联动、天气、成就、细节层。
- 性能压测与降级策略；**连续进出玩法 10 次不崩不残留**。
- **验收**：真机 60 FPS 稳定。

---

## 11. 坑清单（踩过的和必踩的）

1. **缩放模型 → 衣服乱飞。** 缩世界，不缩模型。
2. **一切常数写死** → 换模型/换比例就失灵。一切从身高推导。
3. **相机穿模**是这类玩法的头号体验杀手：必须做碰撞 + 地面钳制 + 按尺度收紧 near clip。
4. **触控区重叠**会让"移动不了"看起来像引擎 bug —— 先划分区域，再排查逻辑。
5. **物理子步数给太低** = 裙摆穿模/抖动；给太高 = 真机发烫。要暴露成可调。
6. **程序化贴图不缓存** → 内存爆 + 卡顿。一次生成、全场景复用。
7. **阴影别贪**：一个投影平行光足够；路灯靠自发光。多个实时光源直接掉一半帧。
8. **退出清理写不全** → 第二次进入残留城市/相机/监听，表现为"越来越卡"。
9. **中文文件名/路径**（中国用户环境很常见）要在导入管线里统一处理。
10. **踩踏判定读骨骼位置的时机**要排在骨骼求解之后，否则永远慢半拍。
11. **步频与速度不同步** → 脚底打滑，一眼假。
12. **别用"关物理"掩盖参数问题**：先把缩放/地面/子步数做对。

---

## 12. 交付纪律（**必须遵守**）

1. **禁止声称"已测试 / 已验证可运行 / 我运行过"**。只能做静态自查。
2. **禁止编造运行结果、截图、日志。**
3. 每阶段提交时必须写清：**这一版解决了什么、怎么验证**（具体到点哪个按钮、看什么现象）。
4. 完成不了的部分**如实说明**，不要假装完成。
5. 报告格式：
   - ① 本次新增/修改文件清单（路径 + 一句话用途）
   - ② 对照需求文档的验收项，逐项标注 `[已实现]` / `[待真机验证]`（没把握的一律标待验证）
   - ③ 我识别到但无法确认的风险点
   - ④ 给用户的操作指引（怎么进玩法、怎么验证）
   - ⑤ 与规格不同的取舍及理由

---

## 13. 版本记录

- **v1.1** —— 同步需求文档 v1.1：移出五项加料；阶段 3 的必须项改为「状态机 + 步态 + 踩踏判定 + 视线与听觉 + 自由沙盒/生存双模式」。
- **v1.0** —— 配套需求文档 v1.0；确立三条工程铁律；明确"缩世界不缩模型"、物理子步数旋钮、出生点校验、零残留退出；
  阶段 3 的必须项为「状态机 + 步态 + 踩踏判定 + 视线与听觉 + 自由沙盒/生存双模式」。
